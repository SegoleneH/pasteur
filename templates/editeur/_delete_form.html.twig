<form method="post" action="{{ path('app_editeur_delete', {'id': editeur.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editeur.id) }}">
    <button class="btn supprBtn">Supprimer</button>
</form>
