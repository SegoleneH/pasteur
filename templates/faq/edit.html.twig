{% extends 'base.html.twig' %}

{% block title %}Modifier Faq {{ faq.id }}{% endblock %}

{% block body %}
    <h1>Modifier Faq {{ faq.id }}</h1>

    {{ include('faq/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_faq_index') }}">Retour à la liste des FAQ</a>

    {{ include('faq/_delete_form.html.twig') }}
{% endblock %}
