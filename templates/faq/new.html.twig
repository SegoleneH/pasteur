{# {% extends 'base.html.twig' %}

{% block title %}Nouvelle FAQ{% endblock %}

{% block body %}
    <h1>Ajouter une FAQ</h1>

    {{ include('faq/_form.html.twig') }}

    <a href="{{ path('app_faq_index') }}">Retour à la liste des FAQ</a>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Nouvelle FAQ{% endblock %}

{% block body %}
    <h1>Ajouter une FAQ</h1>

    {{ form_start(form, {'attr': {'id' : 'faqForm'}}) }}

        {{form_errors(form)}}

        {{ form_label(form.question) }}
        {{ form_widget(form.question, {'attr' : {"class" : 'zoneDeTexte', 'spellcheck' : 'false'} } ) }}
        {{ form_help(form.question) }}

        {{ form_label(form.reponse) }}
        {{ form_widget(form.reponse, {'attr' : {"class" : 'zoneDeTexte', 'spellcheck' : 'false'} } ) }}
        {{ form_help(form.reponse) }}

        {{ form_rest(form) }}

        <button class="btn">{{ button_label|default('Sauvegarder') }}</button>

    {{ form_end(form) }}

        

    <a href="{{ path('app_faq_index') }}">Retour à la liste des FAQ</a>
{% endblock %}
