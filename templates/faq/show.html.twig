{% extends 'base.html.twig' %}

{% block title %}Faq {{ faq.id }}{% endblock %}

{% block body %}
    <h1>Faq {{ faq.id }}</h1>

    <table class="table pure-table pure-table-horizontal">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faq.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ faq.question }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ faq.reponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_faq_index') }}">Retour à la liste des FAQ</a>

    <a href="{{ path('app_faq_edit', {'id': faq.id}) }}">Editer</a>

    {{ include('faq/_delete_form.html.twig') }}
{% endblock %}
