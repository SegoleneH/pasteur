{% extends 'base.html.twig' %}

{% block title %}Praticien{% endblock %}

{% block body %}
    <h1>Praticien</h1>

    <table class="table pure-table pure-table-horizontal">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ praticien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ praticien.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ praticien.prenom }}</td>
            </tr>
            <tr>
                <th>LienRdv</th>
                <td>{{ praticien.lienRdv ? (praticien.lienRdv) : "03.21.75.94.84" }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_praticien_index') }}">back to list</a>

    <a href="{{ path('app_praticien_edit', {'id': praticien.id}) }}">edit</a>

    {{ include('praticien/_delete_form.html.twig') }}
{% endblock %}
