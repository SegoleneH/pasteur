{% extends 'base.html.twig' %}

{% block title %}Metier{% endblock %}

{% block body %}
    <h1>Metier</h1>

    <table class="table pure-table pure-table-horizontal">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ metier.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ metier.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_metier_index') }}">Retour à la liste des métiers</a>

    <a href="{{ path('app_metier_edit', {'id': metier.id}) }}">Modifier</a>

    {{ include('metier/_delete_form.html.twig') }}
{% endblock %}
