{{ form_start(form, {'attr': {'id' : 'metierForm'}}) }}

    {{form_errors(form)}}

    {{ form_label(form.nom) }}
    {{ form_widget(form.nom) }}
    {{ form_help(form.nom) }}
    
    {{ form_label(form.description) }}
    {{ form_widget(form.description, {'attr' : {"class" : 'zoneDeTexte', 'spellcheck' : 'false'} } ) }}
    {{ form_help(form.description) }}
    {{ form_rest(form) }}

    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>

{{ form_end(form) }}